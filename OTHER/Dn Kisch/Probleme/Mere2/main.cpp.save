#include <cstdio>

using namespace std;
int v[4],ind[4],s[4];
int main()
{
    freopen("mere2.in","r",stdin);
    freopen("mere2.out","w",stdout);
    int a,b,c,i;
    scanf("%d%d%d",&v[1],&v[2],&v[3]);
    v[1]=1,v[2]=2,v[3]=3;
    while(1)
    {
    	bool sortat;
    do{
	sortat=true;
    for(i=1;i<3;++i)
        if(v[i]<v[i+1])
        {
        	aux=v[i];
        	v[i]=v[i+1];
        	v[i+1]=aux;
        	aux=ind[i];
        	ind[i]=ind[i+1];
        	ind[i+1]=aux;
        	sortat=false;
        }
    }while(sortat==false);
        for(i=1;i<=n;++i)
             {
             	v[i]=v[i]-(3-i+1);
             	s[ind[i]]=3-i+1;}
             	ap++;
             	if(v[1]==v[2] && v[2]==v[3])
             	  {
             	  	printf("%d\n",ap);
             	  	for(i=1;i<=3;++i)
             	  	  printf("%d ",v[i]);
             	  }
             	  
             
    }	
    }
    return 0;
}
