#include <cstdio>
#define inFile "felinare.in"
#define outFile "felinare.out"
using namespace std
const int LMAX=5000;
bool v[LMAX+5];
int main()
{
    freopen(inFile,"r",stdin);
    freopen(outFile,"w",stdout);
    int n,nr=0;
    scanf("%d",&n);
    for(i=0;i<=n-1;++i)
        if((v[i-1]==0 && i>0)||(v[n-1]==0 && i==0))
            v[i]
    return 0;
}
