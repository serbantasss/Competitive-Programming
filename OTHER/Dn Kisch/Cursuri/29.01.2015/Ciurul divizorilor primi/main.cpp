#include <cstdio>
#include <cmath>
using namespace std;
const int NMAX=1000000
int c[NMAX+5]
inline void desc(int n)
{
  for(int i=2;i<=n;i+=2)
    ++d[i];
  for(int i=3;i<=n;i+=2)
    if(!)
}
int main()
{
    int n,i;
    scanf("%d",&n);
    desc(n);
    return 0;
}
