#include <fstream>
#include <algorithm>
using namespace std;
struct detinut
{
    int x,y;
} v[100001];
bool mycmp(detinut a,detinut b)
{
    return a.x+a.y>b.x+b.y;
}
int main()
{
    int n,i,m,t,ta;
    return 0;
}
