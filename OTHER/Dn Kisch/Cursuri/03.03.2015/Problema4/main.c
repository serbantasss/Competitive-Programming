#include <stdio.h>
#include <stdlib.h>
long long int nr=0,n,c,p=1;
int main()
{
    freope
    scanf("%d",&n);
    while(scanf("%d",&c)!=EOF)
    {
        scanf("%d",&c);
        if(c==1)

    }
    printf("%d",nr);
    return 0;
}
